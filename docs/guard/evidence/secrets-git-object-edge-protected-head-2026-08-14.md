# HOL Guard Secrets Git object edge protected head

This connector-authored commit triggers normal protected pull-request checks after temporary integration automation removed itself.

The exact head must pass Linux, macOS, and Windows wheel and pipx installation, the real Git and pre-commit lifecycle, LFS, submodule, shallow and partial clone, graft, replace-ref, alternate-object, unsupported-encoding, privacy, Security Gates, CodeQL, CI, and resolved-review gates before merge into `release/3.0`.
