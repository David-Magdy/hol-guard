# Attribution

When evaluating adoption, keep these events separate:

1. Claude marketplace plugin install/enable
2. `hol-guard` PyPI/pipx install
3. successful `hol-guard init`
4. active Claude Code protection reported by `hol-guard status`

Only the external marketplace listing is external ecosystem adoption; internal package files are implementation support.
