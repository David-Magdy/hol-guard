# HOL Guard installation reference

Canonical user install:

```bash
pipx install hol-guard
hol-guard init
```

Guard Cloud is optional. Do not require sign-in to enable local Claude Code protection.

For a published 3.x alpha, use the exact version shown on the HOL Guard GitHub prerelease/PyPI release history rather than guessing a version number.
