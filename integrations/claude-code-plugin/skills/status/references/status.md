# HOL Guard status reference

Read-only checks:

```bash
command -v hol-guard
hol-guard --version
hol-guard status
```

Do not infer active protection from this plugin being installed. Use the Guard CLI's reported harness state as the source of truth.
