# Plugin manifest

This manifest exists for Claude Code marketplace validation and discovery. Runtime security behavior is deliberately not implemented in this directory; the `hol-guard` package remains the enforcement authority.
