name: Finalize PR 2325 v5

on:
  push:
    branches:
      - automation/finalize-pr2325-v5
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          ref: feat/secrets-v2-evidence-contracts-clean
          fetch-depth: 1
          persist-credentials: true

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: "3.12"

      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39
        with:
          version: "0.9.26"
          enable-cache: true

      - name: Apply fail-closed contract invariants
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/patch_contracts.py <<'PY'
          from pathlib import Path

          path = Path("src/codex_plugin_scanner/guard/secrets/contracts_v2.py")
          text = path.read_text(encoding="utf-8")

          old_regex = 'r"(?:^|_)(?:raw_?secret|candidate(?:_value)?|credential(?:_value)?|"'
          new_regex = 'r"(?:^|_)(?:raw(?:_value)?|raw_?secret|candidate(?:_value)?|credential(?:_value)?|"'
          if old_regex in text:
              text = text.replace(old_regex, new_regex, 1)
          elif new_regex not in text:
              raise SystemExit("prohibited-key regex not found")

          truncation_guard = (
              '        if self.truncation_codes and not self.partial:\n'
              '            raise SecretContractError("truncation requires partial=true")\n'
          )
          skipped_guard = (
              '        if self.skipped_codes and not self.partial:\n'
              '            raise SecretContractError("skipped work requires partial=true")\n'
          )
          if skipped_guard not in text:
              if truncation_guard not in text:
                  raise SystemExit("truncation invariant not found")
              text = text.replace(truncation_guard, skipped_guard + truncation_guard, 1)

          if "            and not self.skipped_codes\n" not in text:
              anchor = "            and self.error_code is None\n"
              if anchor not in text:
                  raise SystemExit("clean eligibility anchor not found")
              text = text.replace(anchor, anchor + "            and not self.skipped_codes\n", 1)

          if "            bool(self.requested_refs)\n" not in text:
              anchor = "        return (\n            not self.partial\n"
              if anchor not in text:
                  raise SystemExit("clean eligibility expression not found")
              text = text.replace(
                  anchor,
                  "        return (\n            bool(self.requested_refs)\n            and not self.partial\n",
                  1,
              )

          subset_guard = (
              '        if set(self.completed_refs) - set(self.requested_refs):\n'
              '            raise SecretContractError("completed_refs must be a subset of requested_refs")\n'
          )
          if subset_guard not in text:
              raise SystemExit("reference subset invariant not found")

          empty_guard = (
              '        if not self.requested_refs and not self.partial:\n'
              '            raise SecretContractError("empty requested_refs requires partial=true")\n'
          )
          if empty_guard not in text:
              text = text.replace(subset_guard, empty_guard + subset_guard, 1)

          complete_guard = (
              '        if not self.partial and set(self.completed_refs) != set(self.requested_refs):\n'
              '            raise SecretContractError("complete coverage must complete every requested ref")\n'
          )
          if complete_guard not in text:
              text = text.replace(subset_guard, subset_guard + complete_guard, 1)

          path.write_text(text, encoding="utf-8")
          PY
          python /tmp/patch_contracts.py

      - name: Add isolated regression suite
        shell: bash
        run: |
          set -euo pipefail
          cat > tests/test_guard_secret_contracts_v2_regressions.py <<'PYTEST'
          from __future__ import annotations

          import pytest

          from codex_plugin_scanner.guard.secrets.contracts_v2 import (
              PreventionOutcome,
              SecretContractError,
              SecretScanCoverageV2,
              reject_prohibited_fields,
          )


          def _direct_coverage(**overrides: object) -> SecretScanCoverageV2:
              values: dict[str, object] = {
                  "source_set": ("working_tree",),
                  "requested_refs": ("refs/heads/main",),
                  "completed_refs": ("refs/heads/main",),
                  "files_scanned": 1,
                  "bytes_scanned": 32,
                  "commits_visited": 0,
                  "blobs_scanned": 1,
                  "skipped_codes": (),
                  "truncation_codes": (),
                  "detector_version": "guard-secrets-v2",
                  "model_version": None,
                  "cache_hits": 0,
                  "cache_misses": 1,
                  "partial": False,
                  "degraded": False,
                  "error_code": None,
              }
              values.update(overrides)
              return SecretScanCoverageV2(**values)  # type: ignore[arg-type]


          def _mapping_coverage(**overrides: object) -> SecretScanCoverageV2:
              values: dict[str, object] = {
                  "schema": "guard-secret-coverage.v2",
                  "source_set": ["working_tree"],
                  "requested_refs": ["refs/heads/main"],
                  "completed_refs": ["refs/heads/main"],
                  "files_scanned": 1,
                  "bytes_scanned": 32,
                  "commits_visited": 0,
                  "blobs_scanned": 1,
                  "skipped_codes": [],
                  "truncation_codes": [],
                  "detector_version": "guard-secrets-v2",
                  "model_version": None,
                  "cache_hits": 0,
                  "cache_misses": 1,
                  "partial": False,
                  "degraded": False,
                  "error_code": None,
              }
              values.update(overrides)
              return SecretScanCoverageV2.from_mapping(values)


          @pytest.mark.parametrize("key", ["raw_value", "rawValue", "raw-value"])
          def test_raw_value_spellings_are_rejected(key: str) -> None:
              with pytest.raises(SecretContractError, match="prohibited"):
                  reject_prohibited_fields({key: "arbitrary plaintext"})


          @pytest.mark.parametrize("key", ["raw_value", "rawValue", "raw-value"])
          def test_nested_raw_value_spellings_are_rejected(key: str) -> None:
              with pytest.raises(SecretContractError, match="prohibited"):
                  reject_prohibited_fields({"safe": {key: "arbitrary plaintext"}})


          def test_direct_skipped_work_requires_partial() -> None:
              with pytest.raises(SecretContractError, match="skipped work requires partial=true"):
                  _direct_coverage(skipped_codes=("binary_skipped",))


          def test_mapping_skipped_work_requires_partial() -> None:
              with pytest.raises(SecretContractError, match="skipped work requires partial=true"):
                  _mapping_coverage(skipped_codes=["binary_skipped"])


          def test_skipped_partial_coverage_cannot_be_clean() -> None:
              coverage = _direct_coverage(
                  skipped_codes=("binary_skipped",),
                  partial=True,
              )
              assert coverage.clean_eligible is False
              with pytest.raises(SecretContractError, match="cannot produce a clean"):
                  coverage.assert_outcome(PreventionOutcome.CLEAN)


          def test_direct_empty_refs_require_partial() -> None:
              with pytest.raises(SecretContractError, match="empty requested_refs requires partial=true"):
                  _direct_coverage(requested_refs=(), completed_refs=())


          def test_mapping_empty_refs_require_partial() -> None:
              with pytest.raises(SecretContractError, match="empty requested_refs requires partial=true"):
                  _mapping_coverage(requested_refs=[], completed_refs=[])


          def test_empty_ref_partial_coverage_cannot_be_clean() -> None:
              coverage = _direct_coverage(
                  requested_refs=(),
                  completed_refs=(),
                  partial=True,
              )
              assert coverage.clean_eligible is False
              with pytest.raises(SecretContractError, match="cannot produce a clean"):
                  coverage.assert_outcome(PreventionOutcome.CLEAN)


          def test_direct_complete_coverage_requires_all_refs() -> None:
              with pytest.raises(SecretContractError, match="complete every requested ref"):
                  _direct_coverage(
                      requested_refs=("main", "tag"),
                      completed_refs=("main",),
                  )


          def test_mapping_complete_coverage_requires_all_refs() -> None:
              with pytest.raises(SecretContractError, match="complete every requested ref"):
                  _mapping_coverage(
                      requested_refs=["main", "tag"],
                      completed_refs=["main"],
                  )
          PYTEST

      - name: Install locked development dependencies
        shell: bash
        env:
          UV_HTTP_TIMEOUT: "120"
          UV_HTTP_RETRIES: "5"
        run: |
          set -euo pipefail
          if ! uv sync --frozen --extra dev --python 3.12; then
            sleep 10
            uv sync --frozen --extra dev --python 3.12
          fi

      - name: Format, lint, type-check, and test
        shell: bash
        run: |
          set -euo pipefail
          changed=(
            src/codex_plugin_scanner/guard/secrets/contracts_v2.py
            tests/test_guard_secret_contracts_v2_regressions.py
          )
          uv run --no-sync ruff format "${changed[@]}"
          uv run --no-sync ruff check "${changed[@]}"
          uv run --no-sync basedpyright --level error \
            src/codex_plugin_scanner/guard/secrets/contracts_v2.py
          uv run --no-sync pytest \
            tests/test_guard_secret_contracts_v2.py \
            tests/test_guard_secret_contracts_v2_regressions.py \
            tests/test_guard_secret_release_claim_gate.py \
            tests/test_release_toolchain_sbom.py \
            --tb=short

      - name: Reconcile exact test inventory
        shell: bash
        run: |
          set -euo pipefail
          uv run --no-sync python scripts/ci/test_inventory.py --output /tmp/test-inventory.json
          python - <<'PY'
          import json
          from pathlib import Path

          inventory = json.loads(Path("/tmp/test-inventory.json").read_text(encoding="utf-8"))
          baseline = {
              "maximum_collected_cases": inventory["inventory"]["collected_cases"],
              "maximum_test_source_lines": inventory["test_source_lines"],
              "schema_version": 1,
          }
          Path("ci/test-suite-ratchet-baseline.json").write_text(
              json.dumps(baseline, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY
          uv run --no-sync python scripts/ci/test_suite_ratchet.py \
            --baseline ci/test-suite-ratchet-baseline.json \
            --inventory /tmp/test-inventory.json
          git diff --check

      - name: Push validated commit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            ci/test-suite-ratchet-baseline.json \
            src/codex_plugin_scanner/guard/secrets/contracts_v2.py \
            tests/test_guard_secret_contracts_v2_regressions.py
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "fix(secrets): close remaining false-clean contract gaps"
          git push origin HEAD:feat/secrets-v2-evidence-contracts-clean
